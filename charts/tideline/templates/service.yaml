apiVersion: v1
kind: Service
metadata:
  name: {{ include "tideline.fullname" . }}
  labels:
    {{- include "tideline.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tideline.selectorLabels" . | nindent 4 }}
