---
import Layout from '../../layouts/Layout.astro';
import AdminNav from '../../components/admin/AdminNav.astro';
import UserManager from '../../components/admin/UserManager.svelte';
import SessionsPanel from '../../components/admin/SessionsPanel.svelte';
import UserSettingsPanel from '../../components/admin/UserSettingsPanel.svelte';
import { getUsersWithSessionCounts, getAllSessions, getSetting } from '../../lib/db';

const users = getUsersWithSessionCounts();
const sessions = getAllSessions();
const maxSessions = parseInt(getSetting('max_sessions_per_user', '5'), 10);
---

<Layout title="Admin — Users — Tideline">
  <div class="max-w-4xl mx-auto space-y-8">
    <div>
      <h1 class="text-2xl font-bold tracking-tight">Admin</h1>
    </div>

    <AdminNav />

    <section>
      <h2 class="mb-1 text-lg font-semibold">Session limit</h2>
      <p class="mb-5 text-sm text-white/40">
        Maximum number of concurrent sessions allowed per user.
      </p>
      <UserSettingsPanel maxSessions={maxSessions} client:load />
    </section>

    <section>
      <h2 class="mb-1 text-lg font-semibold">Users</h2>
      <p class="mb-5 text-sm text-white/40">
        Accounts that have signed in. Evicting sessions signs that user out everywhere immediately.
      </p>
      <UserManager users={users} client:load />
    </section>

    <section>
      <h2 class="mb-1 text-lg font-semibold">Sessions</h2>
      <p class="mb-5 text-sm text-white/40">
        All active sessions ordered by last activity. Revoke any individual session immediately.
      </p>
      <SessionsPanel sessions={sessions} client:load />
    </section>
  </div>
</Layout>
